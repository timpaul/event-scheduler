services:
  - type: web
    name: event-scheduler
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.0.0
    # disk:
    #   name: data
    #   mountPath: /var/data
    #   sizeGB: 1
